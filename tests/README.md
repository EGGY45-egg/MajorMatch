# Tests

This directory contains lightweight pytest checks for MajorMatch.

## Run

```bash
pytest
```

## What is covered

- CSV corpus loading from one or more files under `data/`
- `semantic_search()` output normalization and metadata shaping
- Query projection behavior for the plot helper
